Python just return s[::-1]

Java: toCharArray. Then define two pointers as 0 and size - 1. Then keep swapping the value of two pointers until left > right. Then convert to String.

Time complexity O(n)