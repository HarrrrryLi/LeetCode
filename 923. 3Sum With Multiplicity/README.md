Sort array first. Then like normal 3sum to record unique combinations. Then using choose(math concept) to calculate how many time each combination appears.


Time Complexity O(N ^ 2). Space Complexity O(N)